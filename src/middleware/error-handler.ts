import { randomUUID } from 'crypto';import { NextFunction,Request,Response } from 'express';
export class PublicError extends Error{constructor(public status:number,public publicMessage:string){super(publicMessage)}}
type DatabaseError={code?:unknown;message?:unknown};
export function isDatabaseError(error:unknown):boolean{if(!error||typeof error!=='object')return false;const value=error as DatabaseError;const code=typeof value.code==='string'?value.code:'';const message=typeof value.message==='string'?value.message.toLowerCase():'';return /^PGRST00[0-3]$|^PGRST30[01]$|^08|^28|^53|^57P0[123]|^58/.test(code)||message.includes('fetch failed')||message.includes('failed to fetch')||message.includes('network request failed')||message.includes('connect')&&(message.includes('refused')||message.includes('timeout')||message.includes('unreachable'))}
export function requestId(req:Request,res:Response,next:NextFunction){const id=String(req.headers['x-request-id']??randomUUID());res.locals.requestId=id;res.setHeader('X-Request-Id',id);next()}
export function errorHandler(error:unknown,_req:Request,res:Response,_next:NextFunction){const id=res.locals.requestId;if(error instanceof PublicError)return res.status(error.status).json({success:false,error:error.publicMessage,requestId:id});if(isDatabaseError(error)){console.error(`[${id}] Database unavailable`);return res.status(503).json({success:false,error:'Database unavailable',message:'Cannot connect to database. Retry later.',requestId:id})}console.error(`[${id}]`,error);return res.status(500).json({success:false,error:'An internal server error occurred',requestId:id})}
