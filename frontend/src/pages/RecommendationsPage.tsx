import { FormEvent,useCallback,useEffect,useState } from 'react';import { Route,X } from 'lucide-react';import RecommendationCard from '../components/RecommendationCard';import { junctionsAPI,officersAPI,recommendationsAPI } from '../services/api';import { Junction,Officer,Recommendation } from '../types';import { PageHead } from './AnalyticsPage';
export default function RecommendationsPage(){const[items,setItems]=useState<Recommendation[]>([]);const[officers,setOfficers]=useState<Officer[]>([]);const[junctions,setJunctions]=useState<Junction[]>([]);const[modify,setModify]=useState<Recommendation|null>(null);const[officerId,setOfficerId]=useState('');const[junctionId,setJunctionId]=useState('');const[note,setNote]=useState('');const[notice,setNotice]=useState('');const load=useCallback(async()=>{const[r,o,j]=await Promise.all([recommendationsAPI.getAll(),officersAPI.getAll(),junctionsAPI.getAll()]);setItems(r.data.data);setOfficers(o.data.data);setJunctions(j.data.data)},[]);useEffect(()=>{load()},[load]);async function accept(id:string){await recommendationsAPI.accept(id);setNotice('Deployment accepted. The assigned officer is now EN_ROUTE.');load()}async function reject(id:string){const reason=prompt('Rejection reason (required):');if(!reason?.trim())return;await recommendationsAPI.reject(id,reason.trim());setNotice('Recommendation rejected and logged.');load()}function openModify(item:Recommendation){setModify(item);setOfficerId(item.officer_id);setJunctionId(item.junction_id);setNote('')}async function save(event:FormEvent){event.preventDefault();if(!modify||!note.trim())return;await recommendationsAPI.modify(modify.id,{newOfficerId:officerId,newJunctionId:junctionId,notes:note});setModify(null);setNotice('Modified recommendation saved to the audit trail.');load()}return <main className="workspace-page"><PageHead eyebrow="Human-in-the-loop deployment" title="Recommendation review" copy="Algorithms propose. Commanders decide. No officer assignment changes until an authorized decision is recorded."/>{notice&&<div className="inline-success">{notice}<button onClick={()=>setNotice('')}><X/></button></div>}<section className="review-stack">{items.length?items.map(item=><RecommendationCard key={item.id} item={item} onAccept={()=>accept(item.id)} onReject={()=>reject(item.id)} onModify={()=>openModify(item)}/>):<div className="empty-state large"><Route/><div><strong>No pending recommendations</strong><span>Run a labeled incident simulation from Live operations to generate one.</span></div></div>}</section>{modify&&<div className="modal-backdrop"><form className="modal" onSubmit={save}><span className="eyebrow">Commander modification</span><h2>Adjust deployment proposal</h2><p>Choose another available officer or target junction and explain why the recommendation was changed.</p><label>Officer<select value={officerId} onChange={e=>setOfficerId(e.target.value)}>{officers.map(o=><option value={o.id} key={o.id}>{o.badge_code} · {o.name} · {o.status}</option>)}</select></label><label>Target junction<select value={junctionId} onChange={e=>setJunctionId(e.target.value)}>{junctions.map(j=><option value={j.id} key={j.id}>{j.junction_id} · {j.name} · Risk {j.current_risk_score}</option>)}</select></label><label>Commander note<textarea value={note} onChange={e=>setNote(e.target.value)} required placeholder="Reason for modifying this recommendation"/></label><div className="decision-actions"><button className="button accept">Save modification</button><button type="button" className="button ghost" onClick={()=>setModify(null)}>Cancel</button></div></form></div>}</main>}
