import { Recommendation } from '../types';
export default function RecommendationCard({item,onAccept,onReject}:{item:Recommendation;onAccept:()=>void;onReject:()=>void}){return <article className="recommendation-card"><div><span className="eyebrow">Human review required</span><h3>{item.recommendation_text}</h3><p>{item.travel_time_minutes} min ETA · expected risk reduction {item.expected_risk_reduction}% · confidence {item.confidence}</p><ul>{item.reasons.map(reason=><li key={reason}>{reason}</li>)}</ul></div><div className="decision-actions"><button className="button accept" onClick={onAccept}>Accept deployment</button><button className="button reject" onClick={onReject}>Reject</button></div></article>}
