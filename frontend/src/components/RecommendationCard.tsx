import { PencilLine } from 'lucide-react';import { Recommendation } from '../types';
export default function RecommendationCard({item,onAccept,onReject,onModify}:{item:Recommendation;onAccept:()=>void;onReject:()=>void;onModify?:()=>void}){return <article className="recommendation-card pro"><div className="recommendation-main"><span className="eyebrow">Pending · Human review required</span><h3>{item.recommendation_text}</h3><div className="deployment-path"><span><small>FROM</small><b>{item.officers?.current_junction_name??'Current patrol position'}</b><em>{item.officers?.badge_code} · {item.officers?.name}</em></span><i>→</i><span><small>TO</small><b>{item.junctions?.name??'Priority junction'}</b><em>Risk {item.junctions?.current_risk_score} · {item.junctions?.current_risk_level}</em></span></div><ul>{item.reasons.map(reason=><li key={reason}>{reason}</li>)}</ul></div><aside className="recommendation-score"><span>DEPLOYMENT PRIORITY</span><strong>{item.deployment_priority}</strong><div><b>{item.travel_time_minutes} min</b><small>Travel time</small></div><div><b>{item.expected_risk_reduction}%</b><small>Expected reduction</small></div><div><b>{item.confidence}</b><small>Confidence</small></div></aside><div className="decision-actions vertical"><button className="button accept" onClick={onAccept}>Accept deployment</button>{onModify&&<button className="button modify" onClick={onModify}><PencilLine/>Modify</button>}<button className="button reject" onClick={onReject}>Reject with reason</button></div></article>}
